{
  "name": "shared-modules-health",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared/modules/health/src",
  "projectType": "library",
  "targets": {
    "typecheck": {
      "options": {
        "commands": ["tsc -p tsconfig.lib.json --noEmit"]
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/shared/modules/health/jest.config.ts"
      }
    }
  },
  "tags": []
}
