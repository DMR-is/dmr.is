'use client'

import {
  AlertMessage as IslandAlertMessage,
  type AlertMessageType as IslandIsAlertMessageType,
} from '@island.is/island-ui/core/AlertMessage/AlertMessage'

export const AlertMessage = IslandAlertMessage
export type AlertMessageType = IslandIsAlertMessageType
