'use client'

import { Footer as IslandIsFooter } from '../../island-is/lib/Footer'

type Props = React.ComponentProps<typeof IslandIsFooter>
export const Footer = (props: Props) => <IslandIsFooter {...props} />
