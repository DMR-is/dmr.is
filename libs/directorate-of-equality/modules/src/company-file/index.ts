/**
 * Public surface of the `company-file` module.
 *
 * The concrete service is deliberately absent: consumers inject
 * `ICompanyFileService` and import the core module, which binds the two.
 */

export * from './company-file.core.module'
export * from './company-file.service.interface'
