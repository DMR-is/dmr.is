import { officialJournalModulesCase } from './official-journal-modules-case'

describe('officialJournalModulesCase', () => {
  it('should work', () => {
    expect(officialJournalModulesCase()).toEqual(
      'official-journal-modules-case',
    )
  })
})
