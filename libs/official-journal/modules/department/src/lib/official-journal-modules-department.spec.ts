import { officialJournalModulesDepartment } from './official-journal-modules-department'

describe('officialJournalModulesDepartment', () => {
  it('should work', () => {
    expect(officialJournalModulesDepartment()).toEqual(
      'official-journal-modules-department',
    )
  })
})
