// Empty page for @view parallel route when on /auglysingar/[type]/[id]
// Returns null to hide the tabs/hero/list on application detail pages
export default function ViewDetailPage() {
  return null
}
